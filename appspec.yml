version: 0.0
os: linux
files:
    - source: /
      destination: /var/www/html/


hooks:
  ApplicationStop:
         - location: /scripts/stop_script.sh
           timeout: 300
           runas: root

   ApplicationStart:
         - location: /script/start_script.sh
           timeout: 300
           runas: root
